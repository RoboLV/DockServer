package app.bundles.core.console;

import dock.framework.BaseComponent;

public class ConsoleComponent extends BaseComponent {
    public ConsoleComponent() {
        setName("Console");
        setDescription("Console manager, provide ability to use console and bind commands");
        setVersion("1.0.0");
    }
}
