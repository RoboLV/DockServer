package app.bundles.core.web.server;

import dock.framework.BaseComponent;

public class WebServerComponent extends BaseComponent {
    public WebServerComponent() {
        setName("WebServer");
        setDescription("Web server to manage HTTP/HTTPS requests");
        setVersion("1.0.0");
    }
}
