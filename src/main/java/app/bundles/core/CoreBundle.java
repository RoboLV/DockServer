package app.bundles.core;

import dock.framework.BaseBundle;

public class CoreBundle extends BaseBundle {
    public CoreBundle() {
        setName("Core bundle");
        setDescription("Core bundle, used as base framework level");
    }
}
